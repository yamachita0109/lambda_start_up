{
  "name": "lambda_template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "standard && mocha --recursive",
    "test-coverage": "standard && nyc --reporter=html --reporter=text mocha --recursive"
  },
  "author": "yamachita0109",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0",
    "log4js": "^6.1.2",
    "request": "^2.88.2",
    "request-promise": "^4.2.5"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "handlebars": "^4.5.3",
    "mocha": "^7.1.0",
    "nyc": "^15.1.0",
    "standard": "^14.3.3"
  }
}
